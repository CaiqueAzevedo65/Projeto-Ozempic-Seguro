# Dependências do projeto Ozempic Seguro
# 
# NOTA: Este arquivo é mantido para compatibilidade com ferramentas que não
# suportam pyproject.toml. A fonte de verdade é pyproject.toml.
#
# Para instalação completa com dependências de dev:
#   pip install -e .[dev]
#
# Para instalação apenas de produção:
#   pip install -e .

# Dependências de produção (sincronizadas com pyproject.toml)
customtkinter==5.2.2
Pillow>=10.3,<12
bcrypt==4.2.1
cryptography==44.0.0